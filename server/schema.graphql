scalar GraphQLDate

type Issue {
  id: Int!
  status: String!
  owner: String
  effort: Int
  created: GraphQLDate!
  due: GraphQLDate
  title: String!
}

type Query {
  about: String!
  issueList: [Issue!]!
}

type Mutation {
  setAboutMessage(message: String!): String
}