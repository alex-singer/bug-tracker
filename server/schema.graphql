type Issue {
  id: Int!
  status: String!
  owner: String
  effort: Int
  created: String!
  due: String
  title: String!
}

type Query {
  about: String!
  issueList: [Issue!]!
}

type Mutation {
  setAboutMessage(message: String!): String
}